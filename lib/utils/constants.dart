import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Honey Theme
  static const Color primary = Color(0xFFF4A460);
  static const Color primaryDark = Color(0xFFD2691E);
  static const Color primaryLight = Color(0xFFFFE4B5);

  // Secondary Colors
  static const Color secondary = Color(0xFF8B4513);
  static const Color secondaryDark = Color(0xFF654321);
  static const Color secondaryLight = Color(0xFFDEB887);

  // Accent Colors
  static const Color accent = Color(0xFF228B22);
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Neutral Colors
  static const Color backgroundLight = Color(0xFFFFFEF7);
  static const Color backgroundDark = Color(0xFF1A1A1A);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color divider = Color(0xFFE0E0E0);
}

class AppSizes {
  static const double radiusSmall = 8.0;
  static const double radiusMedium = 12.0;
  static const double radiusLarge = 24.0;
  
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;
  static const double paddingXLarge = 32.0;
  
  static const double iconSize = 24.0;
  static const double iconSizeSmall = 20.0;
  static const double iconSizeLarge = 32.0;
}

class AppStrings {
  static const String appName = 'Asir Honey';
  static const String currency = 'SAR';
}
